<!-- Thanks for contributing! See CONTRIBUTING.md for the dev setup. -->

## What does this change?

<!-- A short summary. Link the issue if one exists (`Fixes #123`). -->

## How was it tested?

<!-- - `just check` passes locally
     - New tests added for X, Y
     - Manually verified on a ScanNCut DX120 (if relevant) -->

## Checklist

- [ ] `just check` passes
- [ ] New tests cover the change (or N/A for docs/CI)
- [ ] `docs/` and `CHANGELOG.md` updated if user-visible
